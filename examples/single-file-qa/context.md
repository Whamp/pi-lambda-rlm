# MVP note

The Agent Context Avoidance invariant protects the parent agent context budget. The Pi Agent passes a path and question; lambda_rlm reads the source file internally and returns a bounded answer.
